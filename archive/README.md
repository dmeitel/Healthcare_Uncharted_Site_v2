# Archive

Frozen copies of implementations we deliberately replaced. Not built (Eleventy input is `src/`), not deployed. Git history is the real archive; these are the fast-reach copies.

## career-tree-2026-07-12-radial-aoe-iceberg-edu.njk

The full Career Tree source as of 2026-07-12, BEFORE the matrix unification. Holds the two views being replaced:

- **Areas of Expertise, radial atlas style.** Honeycomb wedges around the brand compass rose hub. Zones as angular wedges, groups as territories with rim names, seniority as radius (pips + depth key), zone hulls, zoom-in names, install rings, 4-zone tally HUD, the lens dock, multi-select.
- **Education Matrix, iceberg style.** Stacked degree bands as static HTML rows, credential cards within, family cross-highlighting, detail aside.

Baseline: commit 247d2a8 plus the uncommitted 2026-07-12 session passes (polish, Areas lens, flow pass). If the radial atlas ever comes back as its own thing, start here.
