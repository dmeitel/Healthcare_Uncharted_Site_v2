# Memory Index

- [Run ledger + breakpoint failure map](project_run_ledger.md) — Run 12b (2026-08-22): all three perf fixes re-verified PASS. Run 12 caught the multi-lens Population regression + both pop-in cases
- [Test harness quirks](reference_test_harness_quirks.md) — block `**/.11ty/**`; MapLibre points are canvas-relative (+64px on phone); no `setTimeout`/`globalThis` in the Node scope; throttle after load to expose pop-in; MutationObserver beats polling for intermediate render states
