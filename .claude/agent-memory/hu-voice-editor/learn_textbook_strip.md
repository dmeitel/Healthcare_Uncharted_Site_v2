---
name: learn-textbook-strip
description: 2026-08-27 David-ordered FULL textbook strip of the Learn register; process-engineering is the calibration page; pass covered request-routing, healthcare-data-sources, leading-the-ai-transition, the-payer (57 edits, uncommitted)
metadata:
  type: project
---

2026-08-27: David ordered a FULL TEXTBOOK STRIP of `/learn/*`, not the usual
survive-by-default trim. The writing disappears, headings label, engineered
punchlines go, every fact/number/citation/link stays byte-identical.

**The calibration page is `src/learn/process-engineering/index.html`.** It was
stripped first, by David, and he handed its before/after pairs over as the
quality bar. Read it before starting any further Learn page. The pattern:
aphorism pairs fold into one ordinary sentence, `**Verdict.** explanation`
blocks become plain prose ("The second trap is Conway's Law: ..."), and
first-person that carries real experience SURVIVES with only the staging
calmed.

**Done in this pass (all uncommitted, David has not QA'd):**
`request-routing` 15 edits, `healthcare-data-sources` 18, `leading-the-ai-transition`
13, `the-payer` 11. Em dash count 0 in all four.

**Out of bounds by his instruction:** `/rounds/`, guest pages,
`laws-and-paradoxes` (another agent had it open), `process-engineering`
(already done), sources/reference blocks, disclosures.

**Judgment calls made, worth defending or reversing at QA:**
- request-routing hero opened on four staccato class names ("Break/fix.
  Enhancement. Optimization. Net new.") that duplicate the hero card list two
  inches away. Folded into one sentence; the four classes still arrive in order.
- Pull quotes were rewritten in place, never deleted, so the component keeps
  its content. Where a pull echoed a body aphorism (A10's "book club"), body
  and pull were rewritten to the SAME new sentence, which is the existing
  editorial convention on those pages.
- Fragment tails on the six data-sources category notes were varied rather
  than normalized, so the six siblings no longer share one template.
- Left standing on purpose: A10 "The pilot phase is over. This is
  infrastructure.", the-payer "Fifteen percent of a bigger number is a bigger
  number.", request-routing "If it has a cable or a queue, it is theirs." and
  the drill's quiz copy inside `<script>` (code, out of scope).

**Why:** the site-wide prose audit found Learn DENSER in aphorisms than Rounds
(60%, laws page 81%), and the Learn register per kernel v1.2 is textbook: the
writing disappears.
**How to apply:** when the next Learn page comes in, mirror process-engineering,
not the old trim. See [[voice-keepers-superseded]] before reinstating anything
an older memory calls a budget-holder.
